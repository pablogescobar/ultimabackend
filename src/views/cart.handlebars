<div class="container">
    <h2>ID Carrito: {{cartId}}</h2>
    <ul>
        {{#each cart.products}}
        <li>
            <div class="cartProduct">
                <div>
                    <p>Nombre de Producto: {{this.title}}</p>
                    <p>ID Producto: {{this.productId}}</p>
                    <p>Código de Producto: {{this.code}}</p>
                    <p>Cantidad en Carrito: {{this.quantity}}</p>
                </div>
                <form action="/api/cart/{{../cartId}}/product/{{this.productId}}" method="post" data-method="DELETE">
                    <input type="submit" value="Eliminar producto del carrito">
                </form>
            </div>
        </li>
        {{/each}}
    </ul>
    <a href="/">Volver</a>
    <form action="/api/cart/{{cartId}}/purchase" method="post">
        <input type="submit" value="Finalizar Compra">
    </form>
</div>